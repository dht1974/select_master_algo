

1. 一个选举组里有可以有N(N>=1)个node
2. 每个node有一个全局唯一静态优先级，在选举的时候，如果其他条件都相同的话，静态优先级高的优先
3. 选举时，拥有数据最多node，优先级最高
4. 每个node里，拥有N(N>=1)个组件，每个组件可以进行配置，所有组件全部正常的时候才认为这个node正常
5. 每个node，定时发送本node的信息，本node收到的其他node的信息，本次选举的epoch号等等，采用组播发送
